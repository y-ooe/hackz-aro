# 本番ビルド(npm run build)時に使われる環境変数。
# Cloudflare Pages の Git連携でビルドする場合は、ダッシュボードの
# Environment variables 側に同名の値を設定すればこのファイルは無くてもよい。
#
# バックエンドの公開URL(末尾スラッシュなし)に書き換える。
VITE_API_ORIGIN=https://your-backend-domain.example.com
